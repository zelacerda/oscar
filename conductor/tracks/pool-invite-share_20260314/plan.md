# Implementation Plan: Convites e Compartilhamento de Bolão

**Track ID:** pool-invite-share_20260314
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-14
**Status:** [x] Complete

## Overview

Adicionar campo `description` ao modelo Pool, criar rota `/invite/[code]` com fluxo de entrada automática, e implementar botões de compartilhamento (copiar link + Web Share API) com mensagem personalizada.

## Phase 1: Modelo e API — Campo description

Adicionar o campo `description` ao banco e às APIs de criação/edição de bolão.

### Tasks

- [x] Task 1.1: Adicionar campo `description` (String?, opcional) ao modelo Pool no Prisma schema e rodar migration
- [x] Task 1.2: Atualizar POST `/api/pools` para aceitar e persistir `description`
- [x] Task 1.3: Atualizar PATCH `/api/pools/[id]` para aceitar e persistir `description` (criar endpoint se não existir)
- [x] Task 1.4: Atualizar formulário de criação de bolão (`create-pool-form.tsx`) com campo de descrição
- [x] Task 1.5: Exibir descrição na página de detalhe do bolão (`pools/[id]/page.tsx`)

### Verification

- [ ] Pool pode ser criado e editado com descrição; descrição aparece na tela de detalhe

## Phase 2: Rota de Convite e Fluxo de Entrada

Criar a página `/invite/[code]` que resolve o convite, redireciona para login se necessário, e adiciona o usuário ao bolão.

### Tasks

- [x] Task 2.1: Criar API GET `/api/invite/[code]` que retorna dados públicos do bolão (nome, descrição, admin) a partir do `inviteCode`
- [x] Task 2.2: Criar API POST `/api/invite/[code]/accept` que adiciona o usuário autenticado como membro do bolão (idempotente se já for membro)
- [x] Task 2.3: Criar página `/invite/[code]` com tela de convite mostrando nome/descrição do bolão e botão de entrar
- [x] Task 2.4: Implementar fluxo para usuário não logado: redirecionar para login com `callbackUrl` apontando para `/invite/[code]`
- [x] Task 2.5: Implementar fluxo para usuário já logado: aceitar convite automaticamente e redirecionar para o bolão
- [x] Task 2.6: Garantir que a rota `/invite/[code]` seja acessível sem autenticação no middleware

### Verification

- [ ] Usuário não logado acessa link de convite → vê tela do bolão → faz login → é adicionado ao bolão
- [ ] Usuário já logado acessa link de convite → é adicionado automaticamente e redirecionado

## Phase 3: Botões de Compartilhamento

Refatorar o botão de convite existente e adicionar compartilhamento via Web Share API.

### Tasks

- [x] Task 3.1: Refatorar `CopyInviteButton` para copiar apenas a URL (sem mensagem adicional)
- [x] Task 3.2: Criar componente `ShareInviteButton` que usa Web Share API com mensagem personalizada incluindo nome, descrição e link do bolão
- [x] Task 3.3: Implementar fallback do `ShareInviteButton` para desktop (copiar mensagem completa ou ocultar botão)
- [x] Task 3.4: Integrar ambos os botões na página de detalhe do bolão e na home (onde o admin gerencia seus bolões)

### Verification

- [ ] Botão "Copiar link" copia apenas a URL
- [ ] Botão "Compartilhar" abre share nativo no mobile com mensagem personalizada
- [ ] Em desktop, comportamento de fallback funciona corretamente

## Phase 4: Polish e Instruções OAuth

Ajustes finais e documentação de configuração.

### Tasks

- [x] Task 4.1: Testar fluxo completo end-to-end (criar bolão com descrição → compartilhar → aceitar convite)
- [x] Task 4.2: Verificar se redirect OAuth funciona com a rota `/invite/[code]` e documentar instruções de configuração do Google Console se necessário

### Verification

- [ ] Todos os critérios de aceitação atendidos
- [ ] Instruções de configuração OAuth fornecidas ao usuário (se aplicável)

## Final Verification

- [ ] All acceptance criteria met
- [ ] Tests passing
- [ ] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
