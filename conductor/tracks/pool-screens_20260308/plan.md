# Implementation Plan: Telas de Bolão

**Track ID:** pool-screens_20260308
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-08
**Status:** [x] Complete

## Overview

Implementar três telas para participantes de bolão: home do bolão, formulário de palpites e ranking. Reutilizar os modelos e APIs existentes, criando novos endpoints onde necessário e novas páginas Next.js com controle de acesso.

## Phase 1: API Layer

Criar ou estender os endpoints necessários para suportar as novas telas.

### Tasks

- [x] Task 1.1: Criar `GET /api/pools/[id]/members` — retorna membros do bolão com nome e e-mail
- [x] Task 1.2: Criar `GET /api/pools/[id]/bets` — retorna palpites do usuário autenticado para aquele bolão (categoria + nomeado escolhido)
- [x] Task 1.3: Criar `PUT /api/pools/[id]/bets` — salva/atualiza palpites do usuário autenticado em lote
- [x] Task 1.4: Criar `GET /api/pools/[id]/ranking` — calcula e retorna ranking de todos os membros com pontuação baseada nos resultados registrados

### Verification

- [ ] Todos os endpoints retornam 403 para não-membros
- [ ] `PUT /bets` rejeita com 403 após `lockDate`
- [ ] `GET /ranking` calcula pontos corretamente por tier

## Phase 2: Tela Inicial do Bolão (`/pools/[id]`)

Tela acessível apenas para membros, com conteúdo condicional baseado em `lockDate`.

### Tasks

- [x] Task 2.1: Criar página `src/app/pools/[id]/page.tsx` com controle de acesso (redirecionar não-membros para home)
- [x] Task 2.2: Exibir nome do bolão e lista de participantes
- [x] Task 2.3: Exibir botão "Editar Palpite" linkando para `/pools/[id]/bet` quando `lockDate` não passou
- [x] Task 2.4: Exibir link "Ver Ranking" linkando para `/pools/[id]/ranking` quando `lockDate` passou

### Verification

- [ ] Não-membro acessando `/pools/[id]` é redirecionado
- [ ] Botão "Editar Palpite" aparece apenas antes do lockDate
- [ ] Link "Ver Ranking" aparece apenas após lockDate

## Phase 3: Formulário de Palpites (`/pools/[id]/bet`)

Formulário listando todas as categorias com tier, pontos e seleção de nomeado.

### Tasks

- [x] Task 3.1: Criar página `src/app/pools/[id]/bet/page.tsx` (redirecionar se após lockDate)
- [x] Task 3.2: Listar categorias agrupadas por tier (Ouro → Prata → Bronze → Base) com badge de pontos
- [x] Task 3.3: Para cada categoria, exibir select/radio com os nomeados
- [x] Task 3.4: Pré-preencher form com palpites já salvos do usuário
- [x] Task 3.5: Submeter form chamando `PUT /api/pools/[id]/bets` e exibir feedback de sucesso/erro

### Verification

- [ ] Acesso após lockDate redireciona para `/pools/[id]`
- [ ] Palpites existentes aparecem pré-selecionados
- [ ] Salvar persiste corretamente no banco

## Phase 4: Tela de Ranking (`/pools/[id]/ranking`)

Exibir classificação dos participantes com pontuação calculada em tempo real.

### Tasks

- [x] Task 4.1: Criar página `src/app/pools/[id]/ranking/page.tsx` (redirecionar se antes do lockDate)
- [x] Task 4.2: Exibir tabela de ranking com posição, nome, pontos e número de acertos
- [x] Task 4.3: Adicionar atualização automática da página a cada 60 segundos para refletir novos resultados

### Verification

- [ ] Acesso antes do lockDate redireciona para `/pools/[id]`
- [ ] Pontuação reflete apenas categorias com resultado registrado
- [ ] Página atualiza automaticamente

## Final Verification

- [x] Todos os critérios de aceitação da spec atendidos
- [x] Controle de acesso funciona em todas as telas
- [x] Build sem erros TypeScript (erros pré-existentes não são desta track)
- [x] Fluxo completo implementado: home → palpite → ranking

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
