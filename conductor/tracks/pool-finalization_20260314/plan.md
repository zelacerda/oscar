# Implementation Plan: Finalização de Bolões e Destaque do Vencedor

**Track ID:** pool-finalization_20260314
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-14
**Status:** [x] Complete

## Overview

Adicionar campo `isFinalized` ao modelo `GlobalSettings`, criar API para finalização, adicionar toggle na home do admin (visível apenas quando todas as categorias têm resultado), e modificar a tela de ranking para destacar o vencedor quando finalizado.

## Phase 1: Backend — Schema e API

Adicionar suporte no banco e endpoints para finalização.

### Tasks

- [x] Task 1.1: Adicionar campo `isFinalized Boolean @default(false)` ao modelo `GlobalSettings` no schema Prisma e rodar migration
- [x] Task 1.2: Criar API `GET/POST /api/admin/finalize` — GET retorna `{ isFinalized, canFinalize }` (canFinalize = count results === count categories); POST aceita `{ isFinalized: true }` e só permite se canFinalize for true
- [x] Task 1.3: Expor campo `isFinalized` no endpoint `GET /api/pools/[id]/ranking` para o frontend saber se o bolão está finalizado

### Verification

- [ ] GET `/api/admin/finalize` retorna `canFinalize: false` quando há categorias sem resultado
- [ ] POST `/api/admin/finalize` rejeita finalização quando nem todas as categorias têm resultado
- [ ] POST `/api/admin/finalize` aceita quando todas têm resultado e persiste no banco

## Phase 2: Admin UI — Toggle de Finalização

Adicionar controle de finalização na home do admin, similar ao Controle Global de Votações.

### Tasks

- [x] Task 2.1: Na tela admin (`src/app/admin/page.tsx`), adicionar seção "Finalizar Bolões" com toggle — visível apenas quando `canFinalize` é true
- [x] Task 2.2: Implementar modal/confirmação antes de executar a finalização (ex: "Tem certeza? Essa ação é irreversível.")
- [x] Task 2.3: Após confirmar, chamar POST `/api/admin/finalize` e atualizar o estado visual (toggle desabilitado, indicando que já foi finalizado)

### Verification

- [ ] Toggle não aparece quando há categorias sem resultado
- [ ] Toggle aparece quando todas as categorias têm resultado
- [ ] Confirmação é exigida antes de finalizar
- [ ] Após finalizar, toggle reflete o estado finalizado

## Phase 3: Ranking — Destaque do Vencedor

Modificar a tela de ranking para celebrar o vencedor quando o bolão está finalizado.

### Tasks

- [x] Task 3.1: Modificar `ranking-table.tsx` para receber e usar o campo `isFinalized` da API
- [x] Task 3.2: Quando `isFinalized` é true, exibir destaque especial no primeiro colocado — card maior, mensagem de parabéns, visual dourado diferenciado
- [x] Task 3.3: Ajustar layout para que o destaque funcione bem em mobile

### Verification

- [ ] Ranking sem finalização exibe normalmente (sem destaque)
- [ ] Ranking com finalização exibe primeiro colocado em destaque com parabéns
- [ ] Layout responsivo funciona em mobile

## Final Verification

- [ ] Todos os critérios de aceitação atendidos
- [ ] Fluxo completo: admin registra todos os resultados → toggle aparece → confirma finalização → participante vê vencedor em destaque
- [ ] Testes passando

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
