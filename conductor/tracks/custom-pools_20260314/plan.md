# Implementation Plan: Bolões Customizáveis e Controle de Votação

**Track ID:** custom-pools_20260314
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-14
**Status:** [x] Complete

## Overview

Reestruturar o sistema de bolões para permitir criação por qualquer usuário com categorias/pesos customizáveis, substituir `lockDate` por controle manual de encerramento (dono do bolão + override global do admin). Bolões existentes serão apagados. Novo modelo `PoolCategory` vincula categorias a bolões com pesos customizados.

## Phase 1: Schema e Migração

Alterar o banco de dados para suportar o novo modelo.

### Tasks

- [x] Task 1.1: Criar modelo `PoolCategory` no Prisma (poolId, categoryId, points) para vincular categorias a bolões com pesos customizados
- [x] Task 1.2: Adicionar campo `isLocked` (Boolean, default false) no modelo `Pool` para controle manual de encerramento pelo dono
- [x] Task 1.3: Criar modelo `GlobalSettings` com campo `globalLock` (Boolean) para override do admin global
- [x] Task 1.4: Remover campo `lockDate` do modelo `Pool`
- [x] Task 1.5: Gerar e aplicar migration Prisma; limpar bolões existentes (deleteMany)

### Verification

- [ ] Schema compila sem erros, migration aplicada, banco limpo de bolões antigos

## Phase 2: API — Criação de Bolão

Novo endpoint e lógica para criação de bolões por qualquer usuário.

### Tasks

- [x] Task 2.1: Atualizar `POST /api/pools` — aceitar `categories: {categoryId, points}[]` no body, usar session.user.id como adminId (remover body.adminId), criar registros `PoolCategory` junto com o pool, e adicionar o criador como `PoolMember` automaticamente
- [x] Task 2.2: Criar `GET /api/categories` — retornar todas as categorias com tiers e pesos default para uso no formulário de criação
- [x] Task 2.3: Criar página `/pools/new` com formulário — nome do bolão, seleção de categorias (checkbox) e input de peso por categoria (pré-preenchido com defaults GOLD=10, SILVER=5, BRONZE=3, BASE=1)

### Verification

- [ ] Usuário não-admin consegue criar bolão com categorias e pesos customizados via UI

## Phase 3: API — Controle de Votação

Implementar lock manual e override global.

### Tasks

- [x] Task 3.1: Criar `PATCH /api/pools/[id]/lock` — dono do bolão pode encerrar/reabrir votações (toggle `isLocked`), respeitando globalLock (se globalLock ativo, não permite reabrir)
- [x] Task 3.2: Criar `POST /api/admin/global-lock` — admin global ativa/desativa globalLock em `GlobalSettings`
- [x] Task 3.3: Atualizar `PUT /api/pools/[id]/bets` — substituir checagem de `lockDate` por checagem de `pool.isLocked || globalSettings.globalLock`
- [x] Task 3.4: Adicionar UI de lock na página do bolão (`/pools/[id]`) — botão "Encerrar Votações" visível apenas para o dono do bolão
- [x] Task 3.5: Adicionar botão "Encerrar Todas as Votações" no painel admin (`/admin`)

### Verification

- [ ] Dono encerra votação do próprio bolão; admin global encerra todas; override impede reabertura

## Phase 4: Ranking com Pesos Customizados

Adaptar scoring para ler pesos do banco.

### Tasks

- [x] Task 4.1: Atualizar `GET /api/pools/[id]/ranking` — buscar pesos de `PoolCategory` ao invés do `TIER_POINTS` hardcoded
- [x] Task 4.2: Atualizar `/pools/[id]/bet` — buscar categorias e pesos do `PoolCategory` do bolão ao invés de todas as categorias globais
- [x] Task 4.3: Atualizar exibição de pontos no formulário de palpites para usar pesos customizados do bolão

### Verification

- [ ] Ranking calcula corretamente usando pesos customizados; formulário de palpites mostra apenas categorias do bolão

## Phase 5: Limpeza e Ajustes de UI

Remover referências a lockDate e ajustar telas.

### Tasks

- [x] Task 5.1: Remover referências a `lockDate` em toda a UI (home, pool page, bet page)
- [x] Task 5.2: Atualizar lógica de `isLocked` nas páginas `/pools/[id]` e `/pools/[id]/bet` para usar `pool.isLocked || globalLock`
- [x] Task 5.3: Na home page, mostrar status do bolão (aberto/encerrado) baseado no novo modelo
- [x] Task 5.4: Remover criação de pools do painel admin (agora feito por qualquer usuário via `/pools/new`)

### Verification

- [ ] Nenhuma referência a lockDate no código; UI reflete novo modelo de lock corretamente

## Final Verification

- [ ] Todos os critérios de aceitação atendidos
- [ ] Qualquer usuário cria bolão com categorias/pesos customizados
- [ ] Dono encerra votação; admin global encerra todas com override irrevogável
- [ ] Ranking usa pesos customizados
- [ ] Testes passando (se aplicável)
- [ ] Pronto para review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
