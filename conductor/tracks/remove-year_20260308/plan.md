# Implementation Plan: Remover campo "ano" e simplificar aplicação

**Track ID:** remove-year_20260308
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-08
**Status:** [x] Complete

## Overview

Remover o campo `year` dos 4 modelos Prisma, criar migration para ajustar o banco, mover dados de seed para migration, atualizar APIs, formulários do admin e tela inicial, e alterar o título da barra superior.

## Phase 1: Schema e Migration

Alterar o schema Prisma e criar a migration que remove a coluna `year` e insere os dados do Oscar 2026.

### Tasks

- [x] Task 1.1: Remover campo `year` dos modelos Pool, Category, Nominee e Result no `schema.prisma`
- [x] Task 1.2: Ajustar constraint unique de Category de `[name, year]` para apenas `[name]`
- [x] Task 1.3: Criar migration que remove coluna `year` das 4 tabelas e ajusta constraints
- [x] Task 1.4: Criar migration (ou incluir na mesma) que insere categorias e nomeados do Oscar 2026 via SQL
- [x] Task 1.5: Remover ou simplificar `prisma/seed.ts` (não mais necessário para dados do Oscar)

### Verification

- [x] Banco reflete schema sem campo `year`
- [x] `prisma db push` aplicado com sucesso; migration SQL criada em `20260308140000_remove_year_add_oscar2026_data`

## Phase 2: APIs

Atualizar todas as API routes para remover referências ao campo `year`.

### Tasks

- [x] Task 2.1: Atualizar `POST /api/pools` — remover `year` do body
- [x] Task 2.2: Atualizar `PUT /api/pools/[id]` — remover `year` do update
- [x] Task 2.3: Atualizar `GET /api/categories` — remover filtro por `year`
- [x] Task 2.4: Atualizar `POST /api/categories` — remover `year` do body
- [x] Task 2.5: Atualizar `POST /api/nominees` — remover `year` do body
- [x] Task 2.6: Atualizar `GET /api/results` e `POST /api/results` — remover `year`
- [x] Task 2.7: Atualizar `GET /api/me/pools` — remover `year` do select

### Verification

- [x] APIs atualizadas sem referências ao campo `year`

## Phase 3: UI e Layout

Atualizar formulários do admin, tela inicial e header.

### Tasks

- [x] Task 3.1: Remover campo "Ano" dos formulários e tabelas em `entity-config.ts` (pools, categories, results)
- [x] Task 3.2: Remover exibição de `year` nos cards de bolão em `src/app/page.tsx` (home)
- [x] Task 3.3: Alterar título no header de "Oscar" para "Bolão do Oscar - versão 2026" em `layout.tsx`
- [x] Task 3.4: Footer do admin já exibe "Oscar 2026" — sem alteração necessária

### Verification

- [x] Admin panel sem campo "Ano" nos formulários
- [x] Home exibe cards de bolão sem o ano
- [x] Header exibe "Bolão do Oscar - versão 2026"

## Final Verification

- [x] Todos os critérios de aceitação atendidos
- [x] Schema compilado, banco atualizado, cliente Prisma regenerado
- [x] Fluxo completo funcional: criar bolão, ver categorias, preencher palpites

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
