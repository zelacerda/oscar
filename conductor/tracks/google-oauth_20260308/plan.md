# Implementation Plan: Autenticação com Google OAuth

**Track ID:** google-oauth_20260308
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-08
**Status:** [x] Complete

## Overview

Implementar NextAuth.js com Google Provider no projeto Next.js (App Router). O modelo de usuário atual tem campo `password` que será removido e substituído pelas tabelas padrão do NextAuth (Account, Session). A foto de perfil do Google será salva no banco.

## Phase 1: Setup e Configuração do NextAuth

Instalar dependências e configurar NextAuth.js com Google Provider.

### Tasks

- [x] Task 1.1: Instalar `next-auth` e dependências necessárias (`@auth/prisma-adapter`)
- [x] Task 1.2: Criar credenciais OAuth no Google Cloud Console (documentar variáveis de ambiente necessárias no `.env.example`)
- [x] Task 1.3: Atualizar o schema Prisma — remover campo `password`, adicionar campo `image`, e adicionar modelos `Account` e `Session` do NextAuth
- [x] Task 1.4: Gerar e aplicar migration do Prisma
- [x] Task 1.5: Configurar NextAuth (`src/app/api/auth/[...nextauth]/route.ts`) com Google Provider e Prisma Adapter

### Verification

- [ ] NextAuth responde em `/api/auth/signin` com opção de login Google

## Phase 2: UI de Autenticação

Criar páginas de login e integrar com o fluxo de navegação.

### Tasks

- [x] Task 2.1: Criar página de login (`src/app/login/page.tsx`) com botão "Entrar com Google"
- [x] Task 2.2: Criar `SessionProvider` wrapper no layout raiz para disponibilizar sessão no client
- [x] Task 2.3: Adicionar indicador de usuário logado (nome + foto) e botão de logout no layout

### Verification

- [ ] Usuário consegue fazer login com Google e ver seu nome/foto na interface

## Phase 3: Proteção de Rotas

Proteger rotas da API e páginas que exigem autenticação.

### Tasks

- [x] Task 3.1: Criar middleware Next.js (`src/middleware.ts`) para redirecionar usuários não autenticados para `/login`
- [x] Task 3.2: Proteger rotas da API — verificar sessão nas API Routes e retornar 401 se não autenticado
- [x] Task 3.3: Restringir acesso ao painel admin apenas para usuários com role `ADMIN`

### Verification

- [ ] Rotas protegidas redirecionam para login quando não autenticado
- [ ] API retorna 401 para requests sem sessão
- [ ] Painel admin acessível apenas para usuários ADMIN

## Phase 4: Limpeza e Ajustes Finais

Remover código legado e ajustar integrações.

### Tasks

- [x] Task 4.1: Remover lógica de criação de usuário com senha das API Routes e admin
- [x] Task 4.2: Atualizar seed e scripts existentes para funcionar sem campo `password`
- [x] Task 4.3: Atualizar variáveis de ambiente no `.env.example` com as novas variáveis do NextAuth e Google OAuth

### Verification

- [ ] Aplicação funciona end-to-end: login Google → navegação → logout
- [ ] Nenhuma referência ao campo `password` restante no código

## Final Verification

- [ ] Todos os critérios de aceitação atendidos
- [ ] Testes passando (se aplicável)
- [ ] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
